// Note loaded ----- (PRIME PHASE)
// Short press: Check if arm pos at interface setpoint, run aqui and indexer till primed

// Note primed ----- (SHOOT PHASE)
// Short press: check shooter RPM then drive indexer forewards, then lower arm back down to interface setpoint

// Return to COLLECT PHASE

// Note, try to figure out if we can que an arm setpoint while priming to move ASAP
